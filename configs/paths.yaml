data:
  raw_source: "data/raw/streaming.csv"
  processed_dir: "data/processed/"
  final_dataset: "data/processed/streaming_clean.csv"

artifacts:
  model_dir: "models/artifacts/"
  current_model: "models/artifacts/churn_model_v1.joblib"
  scaler_pipeline: "models/artifacts/pipeline_v1.joblib"

outputs:
  reports_dir: "reports/"
  metrics_file: "reports/metrics.json"
  confusion_matrix: "reports/confusion_matrix.png"
  feature_importance: "reports/feature_importance.png"
