project:
  name: "streaming-churn-prediction"
  version: "1.1.0"
  description: "End-to-end churn propensity model for streaming services"

environment:
  random_seed: 42
  encoding: "utf-8"

business_logic:
  # Threshold for High Risk classification in the dashboard
  risk_threshold: 0.70
  # Used for Estimated LTV calculation
  ltv_horizon_months: 12
  currency: "USD"
